ID: mta
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: java
   type: java
   path: java
   parameters:
      memory: 1024M
   provides:
    - name: java_api
      properties:
         url: ${default-url}
